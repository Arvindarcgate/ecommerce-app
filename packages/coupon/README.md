# coupon

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build coupon` to build the library.

## Running unit tests

Run `nx test coupon` to execute the unit tests via [Jest](https://jestjs.io).
